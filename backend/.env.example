# Supabase Multi-Environment Configuration
# Copy this file to .env and fill in your values

# Development
# Create a separate Supabase project for development
# Get these values from Settings > General > Reference ID
SUPABASE_DEV_PROJECT_ID=your-dev-ref-id

# Staging
# Get these values from your Supabase staging project
# Settings > General > Reference ID
SUPABASE_STAGING_PROJECT_ID=your-staging-ref-id
# Settings > Database > Connection string > password
SUPABASE_STAGING_DB_PASSWORD=your-staging-db-password

# Production
# Get these values from your Supabase production project
# Settings > General > Reference ID
SUPABASE_PROD_PROJECT_ID=your-prod-ref-id
# Settings > Database > Connection string > password
SUPABASE_PROD_DB_PASSWORD=your-prod-db-password


# =====================================
# Supabase Connection (Current Project)
# =====================================
# Get these values from your Supabase project dashboard
# Settings > API > Project URL and anon/service_role keys
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =====================================
# Feature Flags
# =====================================
# Set to 'true' to use Supabase for exercises instead of local exercises.json
USE_SUPABASE=false

# =====================================
# Video Cache Configuration
# =====================================
# Local cache directory for videos downloaded from Supabase Storage
VIDEO_CACHE_DIR=/tmp/exercise_videos
# Maximum cache size in GB (optional, for future cleanup implementation)
VIDEO_CACHE_MAX_SIZE_GB=5

# =====================================
# API Configuration
# =====================================
API_PORT=8000
API_HOST=0.0.0.0
