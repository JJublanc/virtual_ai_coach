# =====================================
# RAILWAY DEPLOYMENT CONFIGURATION
# =====================================
# For Railway deployment, configure these variables in Railway's dashboard:
# 1. Go to your Railway project > Variables
# 2. Add each variable below with your actual values
# 3. Railway will automatically inject $PORT - DO NOT set it manually

# =====================================
# Supabase Connection (REQUIRED)
# =====================================
# Get these values from your Supabase project dashboard
# Settings > API > Project URL and anon/service_role keys
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =====================================
# Feature Flags
# =====================================
# Set to 'true' to use Supabase for exercises (RECOMMENDED for Railway)
USE_SUPABASE=true

# =====================================
# Video Cache Configuration
# =====================================
# Railway uses ephemeral filesystem - /tmp is cleared on restart
# This is acceptable for development but consider external storage for production
VIDEO_CACHE_DIR=/tmp/exercise_videos
# Maximum cache size in GB (optional, for future cleanup implementation)
VIDEO_CACHE_MAX_SIZE_GB=5

# =====================================
# API Configuration
# =====================================
# Railway automatically provides $PORT - do not set PORT manually
# These values are for local development only
API_PORT=8000
API_HOST=0.0.0.0

# =====================================
# Optional: Multi-Environment Configuration
# =====================================
# For advanced setups with multiple Supabase environments

# Development
SUPABASE_DEV_PROJECT_ID=your-dev-ref-id

# Staging
SUPABASE_STAGING_PROJECT_ID=your-staging-ref-id
SUPABASE_STAGING_DB_PASSWORD=your-staging-db-password

# Production
SUPABASE_PROD_PROJECT_ID=your-prod-ref-id
SUPABASE_PROD_DB_PASSWORD=your-prod-db-password
